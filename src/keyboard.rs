pub struct Keyboard {}

impl Keyboard {
    pub fn new() -> Keyboard {
        Keyboard {}
    }

    //    Impliment Key handling later
    pub fn key_pressed(key_code: u8) -> bool {
        true
    }
}